<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/imageViewLogo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:srcCompat="@drawable/logo1550"
            android:transitionName="imageapp"
            />
<!--para hacer la transicion que queremos tenemos que ponerle el transitionName
el nombre de transition name tieme que ser el mismo de la actividad origen que el de la actividad destino-->
        <TextView
            android:id="@+id/textTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:gravity="center"
            android:textColor="@color/black"
            android:text="@string/textTitle"
            android:textSize="32sp"
            android:textStyle="italic"
            android:transitionName="textapp"
            />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>